import React from 'react';
import './BackButton.css';

function BackButton() {
  return (
    <nav className="backButton">
      <ul>
        <li><a href="/">Back</a></li>
      </ul>
    </nav>
  );
}

export default BackButton;